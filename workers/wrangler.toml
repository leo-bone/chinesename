# Cloudflare Worker configuration for Chinese Name Generator API
name = "chinesename-api"
main = "api/generate.js"
compatibility_date = "2024-01-01"

[vars]
# Environment variables will be set via GitHub Actions secrets
